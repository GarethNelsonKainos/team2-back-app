# team2-back-app
Team 2 Backend Application Feb/March 2026

## Environment Setup
Create a `.env` file in the project root and set the `PORT` variable to specify which port the server should listen on (e.g., `PORT=3000`). The value should be a positive integer.